import { cookie } from "./cookie";
import { localStore } from "./localStore";
import { tokenStore } from "./tokenStore";

export const lib = {
  cookie,
  localStore,
	tokenStore,
};

export { cookie, localStore, tokenStore };

export default lib;
